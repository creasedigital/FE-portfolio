import { Mukta, Zeyada } from "next/font/google";

 const zeyada = Zeyada({
    subsets: ["latin"],
    weight: "400"
  });

 const mukta = Mukta({
    subsets: ["latin", "latin-ext"],
    weight: "400"
  });

export { mukta, zeyada };
